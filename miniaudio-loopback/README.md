# miniaudio-loopback

Helper binary for capturing Windows system audio using WASAPI loopback via miniaudio.

Build (Windows):

```
cmake -S . -B build
cmake --build build --config Release
```

The binary is written to `miniaudio-loopback/bin/`.
